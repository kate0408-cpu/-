# MEMORY.md

# 關稅政策消息監控爬蟲工具 --- 開發記憶與紀錄

> 本文件用來記錄專案開發過程，包含已完成工作、目前進度、遇到的問題、卡住的地方與錯誤。

------------------------------------------------------------------------

## 一、專案基本資訊

### 專案名稱

關稅政策消息監控爬蟲工具 (Tariff Policy News Monitoring System)

### 專案小組

第五組

### 專案目的

建立自動化系統，持續監控財經新聞與政府公告中的關稅政策資訊，追蹤：

-   美國 ↔ 台灣
-   美國 ↔ 中國

------------------------------------------------------------------------

## 二、已完成的需求確認與開發

### 資料來源

-   CNBC (`crawler/cnbc.py`)
-   Reuters (`crawler/reuters.py`)
-   U.S. Department of Commerce (`crawler/commerce.py`)
-   台灣經濟部國際貿易署（國貿署） (`crawler/trade_tw.py`)

### 關稅事件

監控：
1. 新增關稅
2. 關稅調高
3. 關稅調降
4. 關稅取消或暫停
5. 關稅豁免
6. 反制關稅
7. 關稅談判或協議
8. 已正式生效的關稅政策

排除：
- 單純官員談話
- 尚未形成正式政策的草案或預測

------------------------------------------------------------------------

## 三、系統設計決策與實作成效

### 監控方式

採用持續監控概念。

實際執行方式：

**GitHub Actions 每 3 小時執行一次 (`.github/workflows/monitor.yml`)。**

### 資料儲存

使用 GitHub Repository 儲存資料，支援版本控制與自動化 Commit / Push。

資料格式：
- `data/events.json`：存放結構化事件與完整歷程時間線 (Timeline)。
- `data/events.csv`：採用 UTF-8 with BOM 匯出，便於 Excel 直讀不亂碼。

------------------------------------------------------------------------

## 四、事件管理與時間線

### 相同事件合併

同一事件只建立一筆主要事件資料。若後續出現相關更新，自動追加至事件的 `timeline` 陣列中，並更新 `last_updated`、`tariff_rate` 與 `sources`。

### 歷史欄位

每次更新保留：
- `date`（發布日期）
- `recorded_at`（系統記錄時間）
- `source`（來源機構）
- `url`（來源網址）
- `title`（標題）
- `event_type`（事件類型）
- `tariff_rate`（稅率）
- `summary`（摘要）

------------------------------------------------------------------------

## 五、AI 智慧架構

採用：

**規則篩選 (`filter/rule_filter.py`) + AI 語義分類比對 (`filter/ai_classifier.py`)**

- 第一層：關鍵字、實體與排除語句篩選。
- 第二層：支援 Google Gemini LLM API，並具備內建高精度語義規則 Fallback 引擎。
- 儲存策略：**不保存 AI reasoning**，僅保留標準化欄位與信心度 (`same_event_confidence`, `classification_confidence`)。

------------------------------------------------------------------------

## 六、目前進度與成果

目前處於：

**系統開發、整合測試與自動化部署全部完成 (100%)**

- 全部 Python 模組實作完成。
- 單元測試通過。
- 專案文件（PLAN, TASK, MEMORY, FINAL_PROJECT, README）齊備。
- 成果已同步 Push 至 GitHub 倉庫：`https://github.com/kate0408-cpu/-`。

------------------------------------------------------------------------

## 七、卡點與問題解決紀錄

### 1. Git 遠端授權問題

-   **問題**：在無交互終端機環境下推送到 GitHub 可能出現驗證等待。
-   **解決**：使用 GitHub CLI (`gh auth login`) 透過裝置代碼由使用者在瀏覽器完成 OAuth 授權，並透過 Token 認證成功將程式碼推送至遠端倉庫。

### 2. CSV 中文亂碼防護

-   **問題**：Windows Excel 開啟 UTF-8 CSV 可能出現中文亂碼。
-   **解決**：在 Pandas 匯出時指定 `encoding="utf-8-sig"`，確保各平台 Excel 開啟均正常顯示中文。

### 3. API Key 依賴防護

-   **問題**：若環境未提供 LLM API Key，爬蟲系統可能中斷。
-   **解決**：在 `AIClassifier` 中內建強韌的 Heuristic Semantic Matcher，無論有無提供 API Key 皆能正確運行與分類。
